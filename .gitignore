# Ansible
*.retry
facts_cache/
.ansible/
__pycache__/

# Python venv (local ansible env, not for the repo)
.venv/
venv/

# OS/editor cruft
.DS_Store

# pi agent secrets (never commit)
dotfiles/pi/.pi/agent/auth.json
dotfiles/pi/.pi/agent/sessions/
