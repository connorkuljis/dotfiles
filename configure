#!/usr/bin/env bash

# opts
set -e

# check if stow is insalled
if ! command -v stow &> /dev/null ; then
	echo "stow is not installed"
	echo "	https://ftp.gnu.org/gnu/stow/"
	exit 1
fi

for dir in */; do
    stow --dotfiles --verbose "${dir%/}"
done
